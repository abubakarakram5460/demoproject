<% if(resource.errors.any?) %>
    <div id="error_explanation">
    <% resource.errors.each do |attr, msg| %>
        <li>
          <%= resource.errors.full_messages_for(attr).first if resource.errors[attr].first == msg %>
        </li>
      <% end %>
    </div>
<% end %>