
<% if @project.errors.messages.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project info from being saved:</h2>

  <ul>
  <% @project.errors.messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
</div>
<% end %>

<%= form_for :projectcode, url: {action: "create"} do |f| %>
    <%= f.label :name %> <br>
    <%= f.text_field :name %> <br>
    <br>
    <%= f.submit %>
    <% end %>